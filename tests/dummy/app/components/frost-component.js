/**
 * Simple re-export of frost-component in the test app namespace
 * (since we don't want it in the real app namespace but it's required to be in the test app namespace for unit tests)
 */
export {default} from 'ember-frost-core/components/frost-component'
