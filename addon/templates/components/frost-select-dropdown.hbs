<div class='frost-select-overlay' onclick={{onClose}} />
<div class={{if (eq top 'auto') 'frost-select-down-arrow' 'frost-select-up-arrow'}} style={{arrowStyle}} />
<div
  class='frost-select-dropdown'
  data-test={{hook (concat receivedHook '-list')}}
  style={{listStyle}}
>
  {{#if multiselect}}
    <div class='multi-status'>
      <span class='number-selected'>{{selectedItems.length}} selected</span>
      <span class='clear' tabIndex={-1} onClick={{action 'clear'}}>Clear all</span>
    </div>
  {{/if}}
  <ul>
    {{#each renderItems as |item index|}}
      {{#frost-select-li
        class=item.className
        data=item
        hook=(concat receivedHook '-item-' index)
        onItemOver=(action 'focusOnItem')
        onSelect=onSelect
      }}
        {{#if multiselect}}
          {{frost-checkbox
            checked=item.selected
            hook=(concat receivedHook '-checkbox-item-' index)
            onInput=onCheck
            size='medium'
            value=item.index
          }}
        {{/if}}
        {{item.label}}
      {{/frost-select-li}}
    {{/each}}
  </ul>
</div>
