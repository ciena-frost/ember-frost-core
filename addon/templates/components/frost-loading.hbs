{{! Template for the frost-loading component }}
{{#if (eq type 'ring')}}
	{{frost-icon
		hook=(concat hook "-loading-ring")
		icon="loading-ring"
	}}
{{else}}
	{{frost-icon
		hook=(concat hook "-loading-ripple")	
		icon="loading-ripple"
	}}
{{/if}}
