{{! Template for the frost-loading component }}
{{#if (eq type 'ring')}}
	{{frost-icon
		hook=(concat hook "-loadingRing")
		icon="loading-ring"
	}}
{{else}}
	{{frost-icon
		hook=(concat hook "-loadingRipple")	
		icon="loading-ripple"
	}}
{{/if}}
